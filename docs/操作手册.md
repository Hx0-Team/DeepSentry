# DeepSentry 操作手册

> 本文档与 [README.md](../README.md) 同步维护，偏向“怎么用”；README 偏向总览、配置和开发说明。

> AI 安全应急 Agent · 控制端 + 远程目标机双视角 · 类 Claude Code TUI

---

## 当前版本重点变化

| 能力 | 用户该怎么理解 |
|------|----------------|
| 模型与上下文向导 | `--init` 内置千帆 Coding Plan、火山方舟 Coding Plan、MiMo Token Plan/MiMo Claw 和自定义接口；可选 auto、64K、128K、256K、512K、1M、2M 或自定义窗口 |
| 工具参数契约 | 内置工具在执行前校验 action、必填参数、枚举和类型；不确定时先调用 `tool_catalog` 获取可执行示例 |
| SSH 主机身份 | 默认 `accept-new` 在首次连接时销定密钥，后续密钥变更会被拒绝；正式环境不要使用 `insecure` |
| 安全归档 | 本地解压/打包防路径穿越、链接越界和解压炸弹；远程直接解压失败关闭，应先下载到控制端处理 |
| 初始化三种模式 | 首次 `--init` 可选择本地、远程 1 台、远程多台 Fleet |
| Shell-first 执行 | 默认优先用目标机原生 Shell；内置工具用于 Shell 不够稳、需要结构化解析或控制端探测的场景 |
| 多目标 Subagent | 对 `targets[]` 批量任务时，每台服务器使用独立连接上下文，避免串目标 |
| 分层长上下文 | 固定保留原始目标、最新用户修正、上一版摘要、核心线索与最近步骤；摘要失败也能继续 |
| 核心线索板 | 自动汇聚 IP、URL、CVE、哈希、路径和明确结论，可用 `/memory clues` 查看 |
| 并发协作 | 子 Agent 获得主目标、TODO 和线索板；任务去重、受控并发，并向主 Agent 结构化交接 |
| TUI 输入优化 | 输入行固定在最后一行，改善 macOS/Windows 中文输入法候选框错位 |
| 长文本粘贴 | 可粘贴大段代码/日志，界面折叠显示，发送给 AI 的仍是完整内容 |
| Claude Code 常用命令 | 支持 `/help`、`/new`、`/clear`、`/status`、`/cost`、`/compact`、`/resume`、`/model`、`/exit` |
| no-tui 思考流 | 默认显示可读思考摘要，不再把完整 JSON action 当思考内容刷屏 |
| 非 JSON 响应恢复 | 模型直接返回 Markdown 时自动识别询问、Shell 代码块和自然语言结论；残缺 JSON 自动重试 |
| Emoji/表格对齐 | 按完整字素簇计算宽度，修复组合 Emoji 与文字重叠、Markdown 表格和询问框右边线错位 |
| sudo 安全交互 | 本机交给系统 `sudo -v` 隐藏验证，远程固定非交互执行；密码不进入输入框、日志或模型上下文 |
| 新运维工具 | `target_health_summary`、`disk_usage`、`file_tail`、`login_audit`、`service_units` |

## 1. 快速开始

### 1.1 安装与编译

```bash
git clone <repo>
cd DeepSentry-deepagent
go build -o deepsentry ./cmd/main.go ./cmd/usage.go ./cmd/survey_compat.go ./cmd/console_other.go
# Windows: 使用 console_windows.go 替代 console_other.go
```

### 1.2 首次运行（配置向导）

```bash
./deepsentry --init
```

向导会询问模型/API 的实际上下文长度，可选自动、64K、128K、256K、512K、1M、2M 或自定义。自定义范围为 4096-4194304 tokens。请以 API 实际限制或本地服务的 `num_ctx` / `max_model_len` 为准；不确定时选“自动”。

向导会引导你配置：

| 步骤 | 说明 |
|------|------|
| AI 服务商 | DeepSeek / Qwen / 千帆 Coding Plan / 火山方舟 Coding Plan / 电信星辰 / 腾讯混元 / OpenAI / Claude / Gemini / MiMo Token Plan / Ollama 等 |
| API 地址 | 支持只填 `/v1`，自动补全 `chat/completions` |
| 模型名称 | 如 `mimo-v2.5-pro`、`deepseek-v4-pro`、`qwen-plus`、`hunyuan-turbos-latest` |
| API Key | 本地 Ollama 可留空 |
| 最大轮数 | 默认 30，防止 Agent 死循环 |
| 管理模式 | 默认本地；Windows cmd 方向键无效时可用 Tab / Shift+Tab 切换 |
| 连接信息 | 根据模式录入 SSH/Telnet/FTP 或多台 `targets[]` |

配置保存至 `./config.yaml` 或 `~/.deepsentry/config.yaml`。

**管理模式说明：**

| 模式 | 结果 |
|------|------|
| 本地任务 | 清空远程连接字段，所有工具在控制端本机执行 |
| 远程管理 1 台服务器 | 写入旧版兼容字段：`target_protocol`、`ssh_*`、`telnet_*`、`ftp_*` |
| 远程管理多台服务器 | 写入 `targets[]`，主进程保持本地控制端模式，由 Fleet/subagent 逐台连接 |

### 1.3 推荐：TUI 模式（类 Claude Code）

```bash
./deepsentry --tui
./deepsentry --tui "排查目标机内存与监听端口"
./deepsentry --plan "配置每 10 分钟 CPU 监控并通知"
./deepsentry --tui -c /path/to/config.yaml --resume session_xxx
```

**TUI 界面说明：**

```
┌─ DeepSentry Agent │ mimo / mimo-v2.5-pro ─────────────────┐
│ ▸ Step 1 / 30                                              │
│ 💭 先查看目标机内存情况...                                  │
│ ┌──────────────────────────────────────┐                   │
│ │ ⚡ Tool · mem_info                    │                   │
│ └──────────────────────────────────────┘                   │
│     └ MemTotal: 1725484 kB ...                             │
├─ SSH -> 8.x.x.x:2222 │ 本地/无活动目标 │ Step 1/30 │ 10:42:05 ┤
│ Enter 发送 · ↑↓ 历史 · Esc 退出输入                         │
│ follow up, Enter to send...                                 │
└──────────────────────────────────────────────────────────────┘
```

| 按键 | 功能 |
|------|------|
| `↑` / `↓` / `j` / `k` | 滚动 Agent 活动日志 |
| `PgUp` / `PgDown` | 翻页 |
| `Ctrl+Home` / `g` | 跳到当前保留记录的顶部 |
| `Ctrl+End` / `G` | 跳到底部并恢复自动跟随 |
| `Tab` | 聚焦底部输入框 |
| `Enter` | 发送任务 / 后续指令 |
| `Shift+Enter` / `Alt+Enter` / `Ctrl+J` | 输入换行（取决于终端是否上报） |
| `Ctrl+U` | 清空当前输入 |
| `Ctrl+L` | 清屏 |
| `e` | 全部展开思考、工具输出和子 Agent 结果；再次按下全部折叠 |
| `Y` | 批准高风险操作 |
| `N` / `Esc` | 拒绝操作 |
| `q` | 仅空闲时退出；运行中不会丢弃任务 |
| `Ctrl+C` | 运行中第一次请求停止并保存 checkpoint，再次按下强制退出 |

**长文本粘贴：** 直接粘贴 1000 行代码/日志时，输入框只显示折叠摘要，提交给 AI 的仍是完整内容。  
**中文输入法：** 当前版本把真实输入行固定为最后一行，候选框会贴近输入行；如果仍显示旧样式，请确认启动的是重新编译后的同一个二进制。
**Markdown 与 Emoji：** 用户询问和最终报告使用同一套 Markdown 渲染；窄窗口中的表格会自动改为键值列表。Emoji 变体选择符、肤色和 ZWJ 组合不会被拆开计算，面板绘制前还会执行最终宽度裁剪。
**模型格式兼容：** 模型偶尔直接返回中文 Markdown 而没有 JSON 外壳时，程序会恢复用户询问或待执行的 Shell 代码块，也可直接呈现自然语言结论；只有残缺 JSON 会触发自动重试。
**sudo 权限：** 本机 TUI 会在需要时暂停全屏，把终端交给系统 `sudo -v` 隐藏读取密码，验证完成后使用 `sudo -n` 执行原命令。验证成功、失败或取消后，程序会显式恢复备用屏幕、鼠标跟踪和全屏重绘，滚轮会继续翻阅 TUI 会话。密码不会进入输入框、日志、报告、Memory 或模型上下文。可在空闲时用 `/sudo` 主动刷新授权。Batch/WebShell 等非交互模式不会索要密码；远程目标只使用免交互 sudo，不会自动复用 SSH 密码。

### 1.3.1 TUI 斜杠命令

输入 `/` 会显示内置命令联想；继续输入前缀可过滤，例如 `/c` 会优先联想到 `/clear`。`↑↓` 切换候选，`Tab` 补全，`Enter` 执行完整命令。

| 命令 | 功能 |
|------|------|
| `/help` | 查看快捷键和命令 |
| `/new` | 开启全新任务/会话；`/new <任务>` 会立即启动 |
| `/restart` | 等同 `/new` |
| `/clear` | 清空当前屏幕日志 |
| `/status` | 查看连接、模型、运行状态 |
| `/cost` | 查看真实 token 用量；模型未返回 usage 时显示估算值 |
| `/compact` | 记录上下文整理提示 |
| `/memory list` | 查看跨会话结构化 Memory |
| `/memory clues [clear]` | 查看或清空当前会话核心线索板；不影响跨会话 Memory |
| `/resume <session_id> [补充说明]` | 在当前 TUI 中恢复并继续 checkpoint，同时重建真实提问和最终结论 |
| `/model` | 查看当前模型 |
| `/sessions` | 列出 checkpoint 会话 |
| `/config` | 显示配置摘要 |
| `/sudo` | 由系统 `sudo -v` 安全验证/刷新本机管理员授权；密码不会进入 DeepSentry |
| `/exit` / `/quit` | 退出 TUI |

### 1.3.2 多轮对话

任务完成后，底部输入框自动激活。可直接输入后续指令（如「再查一下 443 端口」），Agent 会在同一上下文中继续执行。

### 1.3.3 子 Agent 面板

委派子 Agent 时显示黄色边框面板；结果默认折叠，按 `e` 展开完整输出。

Fleet 多目标任务会显示 target 状态，包括 pending/running/ok/error、协议、主机和最后动作。

### 1.3.4 会话选择器

```bash
./deepsentry --tui --pick-session
```

图形化列出 checkpoint 会话（ID、步数、时间），Enter 恢复，N 新建。

### 1.3.5 Benchmark 可视化 TUI

```bash
go run ./cmd/benchmark/ -c config.yaml --tui
```

实时显示 61 个场景进度条与维度得分雷达。

### 1.4 经典 CLI 模式

```bash
./deepsentry --no-tui "分析 auth.log 中的暴力破解 IP"
./deepsentry --no-tui -c config.yaml --batch -y "自动巡检"
./deepsentry --no-tui --json --task "mem_info + port_listen 摘要"
./deepsentry --webshell --task "读取最近登录日志"
```

说明：

- 默认运行会进入 TUI；脚本、WebShell、CI 建议显式加 `--no-tui`。
- `--no-tui` 普通输出会显示 AI 思考摘要，不会把完整 JSON action 作为思考内容刷屏。
- `--json` 输出 JSONL 事件，适合外部程序消费。
- `--webshell` 会提交后台任务并立即返回报告/进度路径：后台子进程启用 `--no-tui --batch -y`，命令 stdout/stderr 会像 fscan 一样逐行写入 `reports/webshell_progress_<timestamp>.log`，完整结果仍写入报告并保留给 Agent；固定索引在 `reports/latest_webshell.txt`。
- WebShell 补充信息恢复示例：`./deepsentry --webshell --resume session_xxx --task "Webhook 是 https://..."`

交互输入时会显示**示例建议**（Tab/方向键可选取）：

- 排查目标机内存与监听端口
- 分析 SSH 登录失败记录
- 审计网络暴露面与异常连接
- 在 Web 目录狩猎 webshell

---

## 2. 命令行参考

```bash
./deepsentry --help
```

| 选项 | 说明 |
|------|------|
| `-c` | 指定配置文件 |
| `--tui` | 全屏 TUI Agent 面板 |
| `--no-tui` | 经典 stdout 输出 |
| `--task` / `-q` | 任务描述，适合脚本调用 |
| `--plan` | 计划模式：必要时先追问，再生成 todo 计划并执行 |
| `--json` | JSONL 事件输出 |
| `--quiet` | 抑制思考流，仅保留关键输出 |
| `--webshell` | 提交后台执行，立即返回报告/进度路径，适合 WebShell/非 TTY |
| `--no-color` | 禁用彩色输出；默认 Windows/macOS/Linux 都启用颜色 |
| `--pick-session` | 配合 `--tui` 图形选择 checkpoint |
| `-batch` | 无人值守（自动批准操作） |
| `-y` | 跳过 batch 二次确认 |
| `-init` | 重新运行配置向导 |
| `--resume ID` | 恢复 checkpoint 会话 |
| `--list-sessions` | 列出可恢复会话 |

**Benchmark 评测：**

```bash
go run ./cmd/benchmark/ -c config.yaml
go run ./cmd/benchmark/ -skip-llm -skip-remote   # 不调用模型和远程目标
```

---

## 3. 工作模式

### 3.1 本地模式、远程单机、Fleet 多机

| 模式 | 触发条件 | 工具视角 |
|------|----------|----------|
| **本地** | 单机连接字段为空，且没有指定远程目标任务 | 所有操作在控制端本机 |
| **远程单机** | 配置 `target_protocol` + 对应 `ssh_*` / `telnet_*` / `ftp_*` | 🎯 目标机 / 💻 控制端 双视角 |
| **Fleet 多机** | 配置 `targets[]` | 主进程本地控制端，按 selector 为每台目标创建连接 |

- **目标机工具**（读 `/proc`、SFTP）：`mem_info`、`port_listen`、`process_list`、`read_file`…
- **控制端工具**（从 DeepSentry 进程发起）：`ping`、`dns_lookup`、`http_probe`、`nmap_scan`…

### 3.2 Fleet 多目标配置

```yaml
targets:
  - name: web-01
    protocol: ssh
    host: 10.0.0.11:22
    user: root
    password: ""
    key_path: ~/.ssh/id_rsa
    tags: ["prod", "web"]

  - name: legacy-router
    protocol: telnet
    host: 10.0.0.2:23
    user: admin
    password: admin
    telnet_prompt: ">"
    tags: ["legacy", "network"]

  - name: ftp-backup
    protocol: ftp
    host: 10.0.0.50:21
    user: backup
    password: xxx
    tags: ["backup"]
```

selector 支持：

| selector | 命中规则 |
|----------|----------|
| `all` | 全部目标 |
| `web-01` | 按 name |
| `10.0.0.11` | 按 host 包含 |
| `ssh` / `telnet` / `ftp` | 按协议 |
| `prod` / `legacy` | 按 tag |

### 3.3 Fleet 与 target-aware 子 Agent

| 方式 | 适合场景 | 示例 |
|------|----------|------|
| `fleet_inventory` | 查看目标清单 | “列出所有 prod 目标” |
| `fleet_exec` | SSH/Telnet 批量执行只读巡检命令 | “对 prod 执行 uptime 和 df -h” |
| `fleet_file` | SSH/Telnet/FTP 文件操作 | “读取所有目标 /var/log/auth.log 末尾 100 行” |
| `task + target_selector` | 每台机器独立分析、最后汇总 | “让 log-analyst 分析所有 prod 机器登录日志” |

`task + target_selector` 示例：

```json
{
  "action": "task",
  "task_name": "log-analyst",
  "target_selector": "prod",
  "task_prompt": "审计最近 24 小时 SSH 登录日志，列出高风险 IP 和成功登录用户"
}
```

注意：

- 每个目标会创建独立 executor，不共享全局连接，避免多机并发串目标。
- 低风险只读任务可自动并发；写入、批量修改、高风险命令需要确认。
- 已配置 targets 时不要让 Agent 裸跑 `ssh/scp/sftp root@host`；这些命令不会读取 DeepSentry 配置里的密码/私钥，请使用 `fleet_exec`、`fleet_file` 或 `target_selector`。
- FTP 目标不能执行 shell，因此 `fleet_exec` 会拒绝 FTP；请使用 `fleet_file` 做 `ls/read/download/upload`。

### 3.4 Agent 动作协议

Agent 每步输出 JSON action，支持：

| action | 用途 |
|--------|------|
| `tool` | 调用内置 Go 原生工具 |
| `execute` | Shell 命令（高风险需确认） |
| `task` | 委派子 Agent；可带 `target_selector` 分解多目标任务 |
| `load_skill` | 加载安全 Skill |
| `todo` | 任务规划 |
| `ask_user` | 缺少关键信息时暂停等待用户补充 |
| `read_file` / `grep` / `ls` / `glob` / `edit_file` | 文件操作 |
| `remember` / `forget` | 跨会话记忆 |
| `finish` | 完成任务 |

### 3.5 跨会话 Memory

DeepSentry 默认内置一份 `builtin://AGENTS.md`，随单二进制一起编译，不需要额外携带文件。

可选扩展：

- `~/.deepsentry/AGENTS.md`：用户级长期偏好
- `.deepsentry/AGENTS.md`：项目级规范
- `~/.deepsentry/memory/store.json`：`remember` / `forget` 写入的结构化记忆

Memory 会自动注入 Agent 上下文；明显的 API Key、密码、Token、私钥等敏感凭证会被拒绝保存。

长期记忆按固定上下文预算注入：优先保留内置安全规则、当前目标记忆和较新的条目，超出预算的内容会明确标注省略，不会无限挤占当前任务的推理空间。

会话内另有“核心线索板”，自动提取高信号的 IP、URL、CVE、哈希、文件路径和明确结论，并保留来源供 Agent 判断是用户提供、执行证据还是推断。它随 checkpoint 保存并提供给主 Agent/子 Agent 协作，但不会自动升级为跨会话永久记忆，避免把一次性噪声长期污染 Memory。

---

## 4. 安全应急场景指南

### 4.1 内存 / CPU 异常

```
排查目标机内存使用，列出前 20 个进程，给出结论
```

推荐工具链：`mem_info` → `process_list` → `finish`

### 4.2 网络暴露面 / 异常连接

```
审计目标机监听端口和 established 连接，识别可疑出站
```

推荐：`port_listen` → `net_connections` → `arp_table`

可先 `load_skill network-recon`。

### 4.3 日志取证

```
分析 /var/log/auth.log 中 Failed password，统计 Top 攻击 IP
```

推荐：`load_skill log-analysis` → `read_log` / `grep` → `finish`

### 4.4 Webshell 狩猎

```
在 /var/www 目录查找可疑 PHP 文件（eval/base64_decode）
```

推荐：`load_skill webshell-hunt` → `glob` + `file_strings` + `file_ident`

### 4.5 漏洞 / 基线核查

```
检查空口令账户、SUID 文件、crontab 后门
```

推荐：`load_skill vuln-scan` → 委派 `vuln-scanner` 子 Agent

### 4.6 本地 + 远程联动

```
从控制端 http_probe 探测目标 80 端口，同时在目标机 port_listen 核对
```

---

## 5. Skills 与子 Agent

### 5.1 内置 Skills（`skills/`）

| Skill | 场景 |
|-------|------|
| `forensics` | 取证分析、/proc 直读 |
| `network-recon` | 网络侦察、连接审计 |
| `log-analysis` | 日志关联、攻击 IP |
| `webshell-hunt` | Webshell 狩猎 |
| `vuln-scan` | 配置基线、弱口令 |
| `ctf` | CTF 题目分析、flag 线索扫描 |
| `awd` | AWD 防守巡检、服务可用性、Webshell/日志证据 |
| `awd-plus` | 多服务态势运营、证据链和处置队列 |

使用：`{"action":"load_skill","skill_name":"forensics"}`

### 5.2 子 Agent（task 委派）

| 名称 | 职责 |
|------|------|
| `log-analyst` | 日志取证 |
| `vuln-scanner` | 脆弱性扫描 |
| `webshell-hunter` | Webshell 检测 |
| `network-analyst` | 网络流量分析 |
| `general-purpose` | 通用多步任务 |
| `ctf-solver` | CTF 解题与证据整理 |
| `awd-defender` | AWD 防守分析 |
| `awd-plus-operator` | AWD-plus 多服务运营 |

使用：`{"action":"task","task_name":"network-analyst","task_prompt":"..."}`

并行委派只适合相互独立的方向。运行器会自动去重相同任务、限制总体并发；每个子 Agent 收到主目标、TODO 和核心线索，并在后续步骤读取共享线索板的新证据。子 Agent 返回“任务状态、核心结论、关键证据、冲突/不确定项、建议下一步”，主 Agent 汇总后更新 TODO。有前后依赖的工作应分两批委派，不应强行放进同一个 `parallel_tasks`。

### 5.3 长上下文、核心线索与并发协作

#### 长上下文如何工作

DeepSentry 会自动管理历史，不需要用户手动复制前情：

| 层级 | 保留内容 |
|------|----------|
| 固定层 | 第一条真实用户目标、最新用户补充/修正、当前核心线索 |
| 摘要层 | 早期命令、关键输出、文件变更、失败原因、TODO、冲突和下一步 |
| 近期层 | `compact` / `balanced` / `full` 默认保留 8 / 12 / 24 条原文，巨大消息仍服从 token 预算 |
| 长期层 | `AGENTS.md` 和 `remember` 保存的跨会话规则/事实，按模型 profile 预算注入 |

当前版本按 `context_window_tokens × context_utilization - system prompt - 本轮工具 schema - 输出预留` 计算历史预算，不再使用固定 60,000 字节阈值。1M 窗口可继续积累大量有效历史；小窗口则更早压缩。摘要会覆盖所有早期分块，大段工具输出不再只保留首尾。

如果摘要模型失败、超时或被停止：

- 保留第一条用户目标和最新修正；
- 保留上一版成功摘要；
- 保留核心线索板；
- 在当前 token 预算内保留尽可能多的最近消息；
- 不会把失败的半成品摘要写回 checkpoint。

本地模型建议在 `config.yaml` 显式填写服务端实际窗口：

```yaml
model_profile: auto
model_parameter_b: 14          # 模型名已含 14b 时可留 0
context_window_tokens: 32768   # 对齐 Ollama num_ctx / vLLM max_model_len
context_utilization: 0         # 0=按 profile 自动
reserved_output_tokens: 0
native_tool_limit: 0
```

启动信息会显示实际 profile、context、输出预留和 Native Tool 直接数量。未声明窗口的本地服务会先按 32K 安全默认运行。

#### 核心线索板

核心线索板属于当前会话，最多保留 48 条高信号候选事实：

- IP、URL、CVE、哈希、Flag、文件路径；
- 明确标注为关键结论、证据、失败原因、冲突或高风险的短结论；
- 每条线索保留来源；同一线索来自多台目标或多个子 Agent 时会合并来源，不会被最后一次结果覆盖；
- 凭证、密码、Bearer Token、私钥等敏感内容不会进入线索板；
- 高优先级标识符会优先保留，普通结论不会无限增长。

查看或清空：

```text
/memory clues
/memory clues clear
```

`clear` 只清空当前会话线索，不会删除 `~/.deepsentry/memory/store.json`、`AGENTS.md` 或历史报告。核心线索会随 checkpoint 保存和恢复，但不会自动变成永久 Memory；需要跨会话保留时再使用 `remember`。

线索板内容是“带来源的候选事实”，不等于全部已经验证。主 Agent 会结合来源区分用户提供、工具证据和推断；跨目标线索未经当前目标复核，不应直接当作本机事实。

#### 多子 Agent 并发规则

当前调度边界：

| 类型 | 最大并发 | 说明 |
|------|---------:|------|
| 本地、彼此独立的子任务 | 4 | 日志、网络、文件等不同方向可并行 |
| 带 `target_selector` 的并行任务 | 3 | 控制远程连接和模型请求压力 |
| 并行任务内部的多目标展开 | 1 | 防止形成“外层并发 × 内层并发”的连接风暴 |

运行器还会：

- 忽略空任务，并对名称、任务内容、目标均相同的委派去重；
- 为每个子 Agent 使用独立执行历史和输出目录，避免长输出互相污染或覆盖；
- 只共享有界核心线索，不复制其他 Agent 的完整原始对话；
- 每个模型步骤前刷新共享线索板，因此运行时间较长的子 Agent 能读取同伴刚发布的证据；
- 停止任务后取消正在运行的模型/命令，排队任务不会继续启动；
- `parallel_tasks` 内任一任务带 `target_selector` 时，整体进入多目标风险确认流程。

推荐并行写法：

```json
{
  "action": "task",
  "parallel_tasks": [
    {
      "task_name": "log-analyst",
      "task_prompt": "只分析今天 auth.log：输出异常 IP、时间线和原始日志证据；完成后停止",
      "task_max_steps": 15
    },
    {
      "task_name": "network-analyst",
      "task_prompt": "只分析当前 established 连接和 DNS：输出可疑远端、PID 和证据；完成后停止",
      "task_max_steps": 12
    },
    {
      "task_name": "webshell-hunter",
      "task_prompt": "只检查 Web 根目录近期修改文件：输出路径、哈希和可疑代码证据；不得修改文件",
      "task_max_steps": 15
    }
  ]
}
```

`task_prompt` 应写清四件事：唯一范围、预期证据、禁止事项、停止条件。不要让多个子 Agent 都执行“全面扫描所有内容”，否则即使任务文本不完全相同，也会产生业务层面的重复劳动。

有依赖关系时使用两阶段流程：

1. 第一批并行完成日志、网络、文件取证；
2. 主 Agent 合并新线索、冲突与失败项并更新 TODO；
3. 第二批只针对第一批发现的 IP、路径或进程做定向复核。

#### 主 Agent 如何接收结果

并行任务结束后，主 Agent 会得到统一交接块：

- 总任务数、成功数、失败数、实际并发和去重数量；
- 每个子 Agent 的耗时、状态和限定长度的结果；
- 本轮新汇聚的核心线索及来源；
- 合并已验证事实、证据链、冲突、不确定项和失败任务的明确要求。

主 Agent 应先更新 TODO，再决定继续执行、发起第二阶段委派、询问用户或结束任务，不应把多个子 Agent 的原始长文本直接重复输出给用户。

---

## 6. 配置详解

参考 `config.example.yaml`：

```yaml
provider: mimo
api_url: https://token-plan-cn.xiaomimimo.com/v1
model_name: mimo-v2.5-pro
api_key: sk-xxx

target_protocol: ssh
ssh_host: 8.x.x.x:2222
ssh_user: root
ssh_password: xxx

# 多机模式使用 targets[]；单机字段可留空
targets:
  - name: web-01
    protocol: ssh
    host: 10.0.0.11:22
    user: root
    tags: ["prod", "web"]

max_steps: 30
llm_retries: 3
ssh_command_timeout: 90
use_native_tools: false

# 可选：TSecBench 跑分平台
benchmark_base_url: https://tsecbench.zc.tencent.com
benchmark_token: xxx
```

订阅套餐可直接使用内置预设：

| provider | Base URL | 默认模型 |
|---|---|---|
| `qianfan` | `https://qianfan.baidubce.com/v2/coding` | `qianfan-code-latest` |
| `volcengine` | `https://ark.cn-beijing.volces.com/api/coding/v3` | `ark-code-latest` |
| `mimo` | `https://token-plan-cn.xiaomimimo.com/v1` | `mimo-v2.5-pro` |

MiMo 官方将 Coding/Agent 工具统一放在 Token Plan，MiMo Claw 包含在该套餐中；暂无独立公开的 Agent Plan Base URL。

**环境变量覆盖：** `DEEPSENTRY_API_KEY`、`DEEPSENTRY_SSH_HOST` 等（前缀 `DEEPSENTRY_`）。

TSecBench 也兼容平台下发的大写配置名 `BENCHMARK_BASE_URL`、`BENCHMARK_TOKEN`，以及同名环境变量。配置完成后可直接让 Agent “跑 TSecBench”，它会使用内置 `tsecbench` 工具完成 list/start/submit/close 流程，并默认隐藏 token。

TUI 中可直接输入 `/tsecbench` 进入跑分模式；首次 `deepsentry --init` 时也会询问是否配置该模式。

### 6.1 基础运维工具速查

| 工具 | 用途 |
|------|------|
| `target_health_summary` | 跨平台健康摘要：系统、负载、磁盘、内存、Top 进程 |
| `disk_usage` | Linux/macOS/Windows 磁盘使用情况 |
| `file_tail` | 大日志尾部读取 |
| `document_parse` | 原生解析 PDF/Word DOCX/Excel XLSX/XLS/CSV/RTF，提取文本、表格和元信息 |
| `login_audit` | SSH/auth.log/secure/last 或 Windows Security 登录摘要 |
| `service_units` | systemd 服务或 Windows Service 列表 |
| `process_list` | Linux `/proc`，Windows 自动降级 `tasklist` |
| `net_connections` | Linux `/proc/net`，Windows 自动降级 `netstat -ano` |
| `pcap_analyze` | gopacket 离线解析 pcap：协议统计、会话、DNS/HTTP/TLS SNI/SMB/NTLM 线索 |
| `tcp_forward` | 授权 TCP 端口映射，类似 iox/lcx 的本地转发，短生命周期 |
| `socks5_proxy` | 授权 SOCKS5 代理，类似 nps/iox 的本地代理能力，仅 CONNECT |
| `schedule_task` | 原生定时任务：解析“明天9点/每天/每周”等自然语言，定时巡检、生成报告并可发钉钉/飞书/邮件网关通知 |

---

## 7. 会话恢复（Checkpoint）

每次 Agent 步骤自动保存至 `~/.deepsentry/sessions/<session_id>/`。

```bash
./deepsentry --list-sessions
./deepsentry --resume session_xxx --tui
```

恢复后 TUI 会显示精简的历史对话轨迹；工具回灌、控制消息和内部动作 JSON 不会混入用户对话。长会话可用 `g/Home` 回到顶部、`G` 回到底部。

LLM 连续失败 3 次也会提示 checkpoint 路径。

---

## 8. 审计与报告

- **实时审计：** `reports/report_<timestamp>.md`（每步 thought / action / output）
- **Benchmark：** `reports/benchmark/benchmark_*.md`
- **TUI 结束：** 面板显示最终报告 + 日志路径

---

## 9. 安全边界

| 规则 | 说明 |
|------|------|
| 受保护路径 | 禁止读 `config.yaml`、`reports/` |
| Workspace 白名单 | `~/.deepsentry/workspace` 可读写 |
| 高风险确认 | `execute` 高危命令需 Y/N；`fleet_exec` / `fleet_file` 会按真实命令或文件动作动态判险 |
| 双视角隔离 | `ping`/`nmap` 不会误走目标机 shell |

---

## 10. 故障排查

| 现象 | 处理 |
|------|------|
| SSH 连接失败 | 检查端口（如 2222 非 22）、`-init` 重配密码 |
| LLM 429/超时 | 增大 `llm_retries`，检查 API 配额 |
| Agent 空转 | 减小任务粒度，或 `--resume` 继续 |
| 长会话频繁整理 | 检查是否持续产生超大命令输出；完整输出会卸载到 workspace，后续应按路径定向读取，不要反复整文件回灌 |
| 摘要失败提示 | Agent 会自动保留目标、上一版摘要、核心线索和最近消息继续；网络恢复后可直接继续，不必新建会话 |
| `/memory clues` 为空 | 当前尚未出现可提取的高信号标识或明确结论；这不表示任务历史丢失 |
| 子 Agent 重复劳动 | 把每个 `task_prompt` 改成互斥范围，并写清预期证据和停止条件；有依赖的任务拆成两批 |
| 并发任务看似变慢 | 多目标任务会主动降低并发，且内部目标展开限制为 1，这是为了避免 SSH/API 连接风暴 |
| 线索出现在错误目标 | 查看 `/memory clues` 的来源标签；来自其他 target 的线索必须在当前目标重新验证 |
| TUI/向导乱码或图标变方块 | Windows `cmd.exe` 和部分国产化终端会自动降级为 ASCII 图标但保留颜色；也可手动设置 `DEEPSENTRY_PLAIN=1` 或 `DEEPSENTRY_ASCII=1`。Windows Terminal/PowerShell 7 可用 `DEEPSENTRY_FANCY=1` 开启漂亮图标 |
| 需要纯无色输出 | 使用 `--no-color`，或设置 `NO_COLOR=1` / `DEEPSENTRY_NO_COLOR=1` |
| 中文输入法候选框错位 | 重新编译并确认启动的是新产物；当前版本输入行固定在最后一行 |
| Emoji 与文字重叠、表格或右边框错位 | 重新构建并确认启动新产物；新版统一使用字素簇宽度。旧终端可设置 `DEEPSENTRY_PLAIN=1` 降级 |
| 正常中文被显示成 `invalid character ...` 解析失败 | 新版会自动恢复普通 Markdown 和 Shell 代码块；若仍连续出现，检查 API 网关是否截断响应并适当增大 `llm_timeout_sec` |
| `config_manage action=view` 提示未知动作 | 新版已兼容 `view/show/list/overview`，都等价于安全的配置摘要 `status` |
| `Password:` 出现在 TUI 最底部并卡住 | 先按 `Ctrl+C`（回到 TUI 后也可用 `Esc`）中断并确认启动最新二进制；新版会自动切到系统 sudo 验证界面。也可空闲时执行 `/sudo`，不要把密码写进命令或对话 |
| sudo 输入密码后滚轮露出 shell/验证历史 | `Password:` 仍在时由系统 sudo 控制终端，输入完成或按 `Ctrl+C` 取消后会自动返回 TUI。若返回后滚轮仍穿透，重新构建并启动最新 `build/deepsentry` |
| 远程 sudo 报 `a password is required` | 出于凭据隔离，远程命令固定使用 `sudo -n`，不会复用 SSH 密码。请用 `visudo` 只为确有需要的命令配置最小范围 `NOPASSWD`，或改用具备权限的账号 |
| 编译后界面还是旧的 | 可能启动了旧二进制。推荐 `bash build.sh && cd build && ./deepsentry -c config.yaml`，或根目录 `go build -o deepsentry ./cmd/main.go ./cmd/usage.go ./cmd/survey_compat.go ./cmd/console_other.go && ./deepsentry -c config.yaml` |
| process_list 慢 | 已优化为限量读取；可调小 `limit` 参数 |
| FTP 批量命令失败 | FTP 不支持 shell，请使用 `fleet_file` 的文件动作 |

---

## 11. 目录结构

```
cmd/main.go          主 CLI + --tui
cmd/benchmark/       能力评测
internal/tui/        TUI 界面（bubbletea）
internal/harness/    Agent 循环 + middleware
internal/builtin/    Go 原生安全工具
skills/              安全 Skill 库
docs/操作手册.md      本文档
```

---

## 12. 典型工作流示例

```bash
# 1. 初始化
./deepsentry --init

# 2. TUI 应急排查
./deepsentry --tui "服务器疑似被入侵，排查内存、端口、最近连接"

# 3. 恢复中断会话
./deepsentry --tui --resume session_abc123

# 4. 无人值守巡检
./deepsentry --batch -y "mem_info + port_listen + net_connections 输出摘要"

# 5. 定时任务守护
./deepsentry --scheduler -c config.yaml

# 6. 能力评测
go run ./cmd/benchmark/ -c config.yaml
```

---

**Hx0 Team · DeepSentry** — 让 AI 安全应急像 Claude Code 一样直观。
